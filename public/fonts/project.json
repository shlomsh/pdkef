{
  "name": "font-assets",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "public/fonts",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run test:fonts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
