{
  "name": "tool-sign",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "src/tools/sign",
  "tags": ["scope:tool", "tool:sign"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run src/tools/sign/",
        "cwd": "{workspaceRoot}"
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx playwright test --project=chromium --project=webkit src/tools/sign/e2e/",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
