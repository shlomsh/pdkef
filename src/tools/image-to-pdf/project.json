{
  "name": "tool-image-to-pdf",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "src/tools/image-to-pdf",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run src/tools/image-to-pdf/",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
