{
  "name": "tool-split",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "src/tools/split",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run src/tools/split/",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
