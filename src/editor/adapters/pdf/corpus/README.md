# The form-field corpus

A table of form elements, each built into a real PDF and run through the whole detector. It exists so
that adding an element is cheap and **the elements already here keep proving themselves for free**
while the detector is refactored around them.

Nx project `form-corpus`. Run it with `npx vitest run src/editor/adapters/pdf/corpus/` or
`npx nx run form-corpus:test`.

```
corpus/
  documents.js   a declarative spec -> a real PDF. Builds only, decides nothing.
  corpus.js      the cases and their expectations. This is the file you edit.
  corpus.test.js the runner. Case-agnostic on purpose; you should not need to touch it.
```

## The paradigm

**One row per element, one expectation per row.** A case names an element (`a comb text field`, `a
clipping rectangle`), gives a spec that produces it, and states how many combs, cells and checkboxes
the pipeline should surface. The runner holds no knowledge of any case, so a new element costs a
row, not a test body. That is the whole design: the cost of adding the twentieth element has to stay
the same as the second, or people stop adding them.

**Real PDFs, never mocks.** Every spec goes through `PDFDocument.save()` and back through
`PDFDocument.load()`, so the detectors read bytes off a parsed document exactly as they do a file
somebody opened. A hand-built object graph would let a case pass while the real parse path was
broken.

**Specs, not committed `.pdf` files.** A binary fixture is opaque in review, cannot say which element
it is exercising, and has to be regenerated by hand whenever the vocabulary grows. A spec is readable
in the diff. The three real documents the corpus also runs are the deliberate exception - see below.

**A zero is an assertion.** Roughly a third of the rows exist only to pin something that must *not*
be detected: a clipping rectangle, an undivided panel, a read-only field, a push button. Those are
the rows that catch a change that makes the detector greedier, which no amount of positive coverage
will.

**`why` is the test name.** It says what the row is pinning, not what it does. When a row goes red,
the failure message should tell you whether the change was wrong or the expectation was.

## The vocabulary

| Group | What it covers |
| --- | --- |
| `live form` | AcroForm widgets: text, comb, multiline, required, read-only, hidden, no-view, checkbox, radio, push button, dropdown, signature - and every visibility flag against *both* field kinds, not just text |
| `printed` | ink in the page's own content stream: comb teeth, boxed combs, painted squares, ruled rows, panels, clipping paths |
| `hybrid` | both sources describing one field, which is what a real fillable form is |
| `page geometry` | rotation, a crop box away from the origin, and page indices across a multi-page document |
| `known gap` | what the detector does *not* do today, pinned on purpose - see below |

## Adding an element

1. Add a spec kind to `documents.js` if the vocabulary cannot already express it. Keep it declarative:
   the builder draws, it never decides.
2. Add a row to the right group in `corpus.js`, with a `why` that says what it is pinning.
3. Run it, and **read the number before you write it down**. An expectation copied from a passing run
   you did not understand is worse than no row at all.
4. If your row pins a *limitation* rather than a desired behaviour, put it in `known gap` and say in
   `why` where the evidence lives and what fixing it should change the row to.

## Two things to know before you trust a number

**The runner leaves out the pdf.js text pass.** `useFormFieldRegions` feeds real text runs to
`formCells.js`, which uses them for label lookup and for its "this cell is explanatory, drop it"
filter. The corpus passes `[]`. The geometry the corpus is about does not read text at all, and
including it would make every row depend on a second parser and on whatever prose a fixture happens
to carry. The cost is that a few cells a real page would filter out survive here. If `detectPage` in
the runner ever drifts from the hook, the corpus is measuring something the product does not do.

**pdf-lib never emits the `re` operator.** It builds every rectangle as `m/l/l/l/h`, so a
`drawRectangle` lands in `ink.verticals`/`ink.horizontals` and never in `ink.rects` - which is the
only thing `findCheckboxes` reads. That is why `paintedRect` exists as a separate spec kind, pushing
raw operators. Real producers do both, which is why `pageInk.js` normalizes both, so a corpus that
could only express one half would leave a detector uncovered. Our own practice form is pdf-lib's
output and has exactly the path-drawn shape.

## One row per flag, per field kind

The `live form` group looks repetitive on purpose: hidden, no-view, read-only and print each get a
row for a text field *and* a row for a checkbox. That is not padding. The visibility rules were
written for `/Tx` and `/Btn` went on being read straight off `/Annots` with none of them, so a hidden
checkbox stayed a mark target long after a hidden text field stopped being one - a gap no amount of
text-field coverage could have shown. Both kinds now answer through one function
(`visibleWritableRect`), and the paired rows are what keeps them answering the same way.

When you add a field kind, add its rows for every flag, even where you are sure of the answer.

## Known gaps

These rows assert what the detector does today, not what it should do. They are here so that fixing
one shows up as a failing row to update rather than as a silent change, and so nobody re-discovers
the same limit from scratch. Each row's `why` names where the evidence lives.

Today: a checkbox square stroked as a path is never a checkbox candidate (the miss behind "none of
the drawn squares" on form 101 in `docs/mobi-10-field-map-spike.md`), and a real `/Sig` field is
invisible because signature placement is a different creation mode.

## The real documents

Three files, run alongside the synthetic rows, because a synthetic case proves one rule in isolation
and only a file somebody was actually sent proves the rules hold together:

| File | Why it is here |
| --- | --- |
| `public/images/redaction-guide/sample.pdf` | our practice form - a live AcroForm, 9 fields, the file the home page offers on a first visit |
| `__fixtures__/income-tax-101-page1-geometry.pdf` | a dense flat form, no widgets at all |
| `__fixtures__/health-declaration-page1-geometry.pdf` | the other scored flat form, whose checkboxes are painted rects rather than paths |

The two flat forms carry no widget, which is what makes the widget pass a provable no-op on them -
and therefore what lets the recall and precision numbers in `docs/mobi-10-field-map-spike.md` stand
unchanged since the widget source was added. The corpus asserts that directly.

## What the corpus is not

It is not a recall or precision measurement. Those numbers come from scored ground truth on real
forms (`docs/mobi-10-field-map-spike.md`, `scripts/spike/mobi-10/score.mjs`) and a synthetic case
cannot contribute to them: a fixture built to be detected proves the rule, not the rate. The corpus
answers "does this element still behave the way we decided", and the spike record answers "how often
does the detector find a real form's fields". Do not let a green corpus be read as the second thing.

## Why this is its own Nx project

`nx.json` narrows CI to what a change touches. `editor` is a core project, so any change inside it
runs everything anyway; the project boundary buys one thing, which is that a **corpus-only** change
runs only the corpus instead of the whole editor suite. Modest, and worth being honest about. The
other reason is identity: this is a named thing with a doc, a vocabulary and a way of being extended,
and that is easier to find and to keep than a test file in a folder of test files.

Nesting a project inside `editor` means `editor:test` (which runs `src/editor/`) also runs these
tests, so under Nx they can run twice. That is the same shape as `cross-tool-tests` inside
`site-test`, and the affected-scope oracle reads project roots straight from the Nx graph, so nothing
in `scripts/affected-scope.mjs` needs to know this project exists.
