import type { ElementDefinition } from './types.ts';
export const signatureDefinition: ElementDefinition = { type: 'signature', resizeBehavior: { handles: ['top-left', 'top-right', 'bottom-left', 'bottom-right'] } };
