import type { ElementDefinition } from './types.ts';
export const textDefinition: ElementDefinition = { type: 'text', resizeBehavior: { handles: ['top-left', 'top-right', 'bottom-left', 'bottom-right'] } };
