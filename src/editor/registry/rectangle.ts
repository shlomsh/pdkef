import type { ElementDefinition } from './types.ts';
export const rectangleDefinition: ElementDefinition = { type: 'rectangle', resizeBehavior: { handles: ['top', 'right', 'bottom', 'left', 'top-left', 'top-right', 'bottom-left', 'bottom-right'] } };
