import type { ElementDefinition } from './types.ts';
export const ellipseDefinition: ElementDefinition = { type: 'ellipse', resizeBehavior: { handles: ['top', 'right', 'bottom', 'left', 'top-left', 'top-right', 'bottom-left', 'bottom-right'] } };
