import type { ElementDefinition } from './types.ts';
export const symbolDefinition: ElementDefinition = { type: 'symbol', resizeBehavior: { handles: ['top-left', 'top-right', 'bottom-left', 'bottom-right'] } };
