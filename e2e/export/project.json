{
  "name": "export-guards",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "e2e/export",
  "implicitDependencies": ["font-assets", "editor", "lib", "tool-sign"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx playwright test --project=export-guards e2e/export/",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
