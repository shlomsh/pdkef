{
  "name": "fonts",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "e2e/sign",
  "implicitDependencies": ["font-assets"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx playwright test --project=fonts e2e/sign/",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
