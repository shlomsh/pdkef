// A second file mentioning the names this guard's single-owner rule reserves
// to one owner - even in a comment like this one, since the rule is a plain
// text scan, the same as the `git grep -l` step it replaced.
// maxWidthFromRightGrowth maxHeightFromBottomGrowth
export const NOTE = 'see boxResize.ts';
